import numpy as np
import pandas as pd

def add_one(x: float) -> float:
    return np.array(x) + 1


def add_two(x: float) -> float:
    return np.array(x) + 2

def add_three(x: float) -> float:
    return np.array(x) + 3
